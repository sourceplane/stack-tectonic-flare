apiVersion: sourceplane.io/v1
kind: Component

metadata:
  name: stack-tectonic-flare

spec:
  type: publish-stack
  domain: platform-stack
  subscribe:
    environments:
      - name: dev
        profile: dry-run
      - name: staging
        profile: release
      - name: production
        profile: release
  inputs:
    registry: ghcr.io/sourceplane/stack-tectonic-flare
    root: .
    version: "1.0.0"
  labels:
    layer: platform
    provider: orun