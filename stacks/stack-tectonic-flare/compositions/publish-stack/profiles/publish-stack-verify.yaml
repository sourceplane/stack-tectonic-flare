apiVersion: sourceplane.io/v1alpha1
kind: ExecutionProfile
metadata:
  name: publish-stack-verify
spec:
  description: Staging verification — validates publish without pushing to registry

  jobs:
    publish:
      includeCapabilities:
        - publish-stack.setup
        - publish-stack.dry-run
