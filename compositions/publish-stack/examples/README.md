# Examples for publish-stack

These fixtures show how `publish-stack` looks inside a consumer repository.

- `examples/stack-tectonic` — the Stack Tectonic stack itself, published to `ghcr.io/sourceplane/stack-tectonic`
