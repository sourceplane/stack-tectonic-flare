# Examples for publish-stack

These fixtures are excerpted or adapted from `example-platform-repo` to show how `publish-stack` looks inside a consumer repository.

- `examples/component.yaml`
- `examples/config.yaml`
