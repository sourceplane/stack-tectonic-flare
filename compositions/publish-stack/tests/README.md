# Tests for publish-stack

These files provide contract or smoke fixtures that CI can inspect for `publish-stack` without requiring a full consumer repository checkout.

- `tests/smoke`
- `tests/test.yaml`
