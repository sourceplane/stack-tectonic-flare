# Tests for cloudflare-worker

These files provide contract or smoke fixtures that CI can inspect for `cloudflare-worker` without requiring a full consumer repository checkout.

- Add a smoke or contract fixture under tests/.
